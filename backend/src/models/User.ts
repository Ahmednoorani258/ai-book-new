// User model based on Better Auth's internal user representation
// This is a placeholder and should be integrated with Better Auth's user management
export interface User {
  id: string;
  // Other user attributes managed by Better Auth, e.g., email, password hash
}
